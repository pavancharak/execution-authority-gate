# Generation
openai==1.3.0

# Detection
# Must match whatever produced the committed detect/models/detector.pkl
# (see pipeline/src/build_demo_data.py / run_pipeline.py) — pickle isn't
# forward/backward compatible across major numpy/sklearn versions, and
# the deployed image now actually unpickles this model live (interactive
# demo's Live Test Harness), not just at training time.
scikit-learn==1.9.0
numpy==2.4.6

# Cryptography
cryptography==41.0.0

# Web
flask==2.3.0
flask-cors==4.0.0

# Utilities
requests==2.31.0

# Testing
pytest==7.4.0
pytest-cov==4.1.0
