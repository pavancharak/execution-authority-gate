# pipeline/ composes detect/, mandate/, and sign/ directly (see the
# sys.path wiring at the top of run_pipeline.py), so it needs their deps.
# mandate/ is stdlib-only and needs nothing extra.
scikit-learn==1.3.0
numpy==1.24.0
cryptography==41.0.0
